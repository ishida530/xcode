import { Routes } from '@angular/router';
import { DashboardCurrencyComponent } from './component/dashboard-currency/dashboard-currency.component';

export const routes: Routes = [
    { path: '', component: DashboardCurrencyComponent },
];
